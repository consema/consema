/target/
/.pi-tmp/
**/*.rs.bk
# 日志忽略只锚定 fuzz 账本目录（2026-08-15 波 5 归正）：非锚定 `*.log` 会静默吞掉
# 任何新位置的证据日志（GW4-23 已记录该失败模式并只修补 docs/release/ 一处）；
# 锚定后新日志位置不再被 ignore 吞掉——git status 直接可见，提示显式跟踪决策。
/docs/fuzz-evidence-0.13.0-logs/*.log
!/docs/fuzz-evidence-0.13.0-logs/waves.log
!/docs/fuzz-evidence-0.13.0-logs/wave-*.out.log
!/docs/fuzz-evidence-0.13.0-logs/wave-*.err.log
!/docs/fuzz-evidence-0.13.0-logs/workspace-gate-*.log
# 发布证据日志（GW4-23，2026-08-15）：docs/release/*.log 由发布记录显式跟踪，
# 不受上述目录限定忽略影响；目录级 git add 会把新发布日志显示为未跟踪。
!docs/release/*.log
# HCL Go oracle driver 构建产物（2026-08-15 波 4）：默认 DriverPath 已移至
# target\oracles；旧内树路径供显式 -DriverPath 用法兜底，忽略以免弄脏工作树。
conformance/oracles/hcl-go-v1/hcl-go-differential.exe
*.tmp
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Monorepo split leftovers (2026-08-12: sources moved to consema-rs/-go/-ts/-py/-kt)
/crates/
/python/
/typescript/

# Scratch working notes (2026-08-14: prep2b.txt / _tmp_note.txt / _tmp_targets.txt
# 为临时工作笔记，已删除；兜底忽略，避免再入仓)
prep2b.txt
_tmp_note.txt
_tmp_targets.txt

# Kotlin / Gradle build output
kotlin/build/
*.class

# Python caches, virtualenvs, packaging
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
*.egg-info/

# Node.js
typescript/node_modules/
node_modules/

# Claude Code local state — only .claude/workflows/adversarial-audit.js is tracked
# (covers settings.local.json / scheduled_tasks.lock; 换机克隆不依赖全局配置)
.claude/*
!.claude/workflows/
.claude/workflows/*
!.claude/workflows/adversarial-audit.js
