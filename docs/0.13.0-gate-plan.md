# Consema 0.13.0 门禁执行计划（Rust 生产加固与 Feature-Complete Gate）

- 对应规范：路线图《Consema 1.0.0 产品路线图与双语言落地设计》§14.12（0.13.0 范围）、§15（Rust Feature-Complete Gate 全七节）、§19.4（发布供应链）、§20（性能与可扩展性）、§23（真实项目验证）；SECURITY.md（no-secrets 与依赖政策）；RFC 0011（semantic-model v6 与 contract registry）
- 目标版本：0.13.0（对齐路线图 §14.12；只有第 15 节门禁全部通过，才允许开始 0.14.0 Go 实现，§14.12 第 1345 行）
- 先例：`docs/cli-implementation-plan.md`（0.12.0）与 `docs/hcl-implementation-plan.md`（0.11.0）的多 agent 文件域计划体例
- 语义权威顺序（沿用 `docs/IMPLEMENTATION.md`）：永久不变量 → 已接受 RFC → 语言无关 conformance vectors → 本实现计划与 Rust API → 第三方行为仅为实现细节
- 本文档是 0.13.0 门禁的执行计划：逐条核对第 15 节清单的现状（全部证据 file:line），列出缺口，拆分里程碑与 agent 文件域，并定义 Feature-Complete Manifest 的形状。除已标注的并行轨交付物外，本计划不假设任何未落盘的改动。
- **拆分后行号注记（2026-08-13）**：本文为 2026-08-07 规划/记录文档，文中 `README.md:NN`、`Cargo.toml:NN` 与 `.github/workflows/ci.yml:NN` 均为拆分前单仓布局的行号，六仓拆分后不适用——10-job ci.yml 归 consema-rs 仓（consema-rs/.github/workflows/ci.yml），母仓 ci.yml 现为 oracles/shared-conformance-digest/check 三 job；README 现行 conformance 声明在 :56、上游套件清单在 :65；508 cases 计数为 2026-08-12 P2-B 补强前值，现行为 18 suites / 519 cases（digest cfd6e296…）。

---

## 0. 总体结构

### 0.1 现状核查（本计划调研结论，2026-08-07）

**已通过的基础门禁（本地实测或仓库记录，详见 §2 证据表）：**

- 18 套语言无关 conformance suite 508/508（2026-08-12 P2-B 向量补强后为 519/519，digest cfd6e296…）：`conformance/vectors/` 下 18 个 vector 文件，合计 508 cases（补强后 519，本计划逐文件计数复核）；记录见 `README.md:56`、`CHANGELOG.md:17`。
- `cargo test -p consema-conformance --locked` 本地实测全绿（2026-08-07，58 个 lib tests + 74 个 integration tests，含 hardening/fixtures/publication/encoding corpus 全部 test 文件）。
- `cargo deny check advisories bans licenses sources` 本地实测全绿（deny.toml 现行配置）。
- `cargo audit --no-fetch` 本地实测：加载 1,189 条 RustSec advisory，扫描 76 个依赖，0 漏洞（Cargo.lock 实测 76 个 `[[package]]`；cargo audit 口径随版本变化，以当前扫描结果为准）。
- 恶意/边界硬化测试 55 个（SECURITY.md:16：hardening.rs 12、yaml_hardening.rs 5、line_formats_hardening.rs 6、xml_hardening.rs 10、xml_encoding_corpus.rs 7、plist_hardening.rs 7、hcl_hardening.rs 8）。
- 上游/官方套件：toml-test v2.2.0 205 valid + 474 invalid（SECURITY.md:30）、yaml-test-suite data-2022-01-17 402 case（README.md:37）、JSON5 v2.2.3 语料 83/83（README.md:35）、5 套 INI/Properties runtime oracle 36/36（README.md:38）。
- 差分 oracle：HCL Go differential 29 case、plist macOS differential 7 case，均有 manifest 记录与文档化 skip 路径（`conformance/oracles/hcl-go-v1/manifest.json`、`conformance/oracles/plist-macos-v1/manifest.json` 的 `skip_path`）。
- 性能基线：`docs/BENCHMARKS-0.6.0.md` ~ `docs/BENCHMARKS-0.11.0.md` 共 6 份（逐格式 parse/project/materialize/edit 基线）。
- `unsafe_code = "forbid"` + `missing_docs = "warn"` workspace lints（Cargo.toml:36-38）；MSRV 1.85 在 manifest 声明（Cargo.toml:33），本地 rustup 已装 1.85.0。
- RFC 0001-0015 共 15 份（`docs/rfcs/`）；contract registry 38 条 + 166 个 error code（README.md:33）。
- git 标签 v0.1.0..v0.8.0 存在（`git tag -l`；v0.8.0 指向 f79dd99），是 semver 审查的基线锚点；注意：全部为 annotated 但**未签名**（`git cat-file -p v0.8.0` 无 gpgsig 头，见 G-10）。

**缺口（详见 §3 缺口清单；下方为本计划调研时的初始状态，M1-M8 已逐项关闭，§3 已标注"已交付"）：**

- 仓库没有任何 CI（`docs/cli-implementation-plan.md:18` "仓库无 `.github/`：CI 是本地门禁"）；三平台 CI 与 MSRV 真实验证（§15.6）全部为空。
- 打包验证脚本 `scripts/verify-package-archives.ps1`：无 `--allow-dirty`（脏树不可跑）、无 MSRV 构建腿（0.8.0 发布记录的 "MSRV 1.85 build" 未落进脚本）；本文档对应交付已补（见 §4 M1）。
- fuzz/property/mutation 语料：仓库无 `fuzz/`、无 proptest 依赖（Cargo.lock 检索确认）；`conformance/corpora/` 只有 json5-v2.2.3.json 与许可证文本。SECURITY.md:16 所称 "property tests" 实为确定性硬化测试（固定输入、固定次数），不是 property-test 框架。§15.3 的持续 fuzz 与 72 CPU-hours RC 证据为空。
- coverage 工具链：仓库无 llvm-cov 配置；84.65% region 是单次辅助报告（CHANGELOG.md:92、RELEASE-0.8.0.md:98），不可复现。
- API/semver 审查：仓库无 cargo-semver-checks 配置；五维审计的命名漂移 findings（F2/F3/F4/F10/F11 等）**仓库内无记录**（检索 docs/、CHANGELOG.md、memory 均无），是 §15.6 独立 API 审查的输入 backlog，需先转写（§4 M4）。
- 性能/内存预算：BENCHMARKS 是"可复现基线"（BENCHMARKS-0.8.0.md 明示"not a release threshold"），§15.5 要求的正式 p50/p95/峰值内存预算与 10% 回退政策尚未冻结。
- 真实项目 pilot（§23）、发布供应链（§19.4：SBOM、签名、build provenance、恢复演练）全部为空。
- Feature-Complete Manifest（§15.7）：不存在。

### 0.2 与 0.12.0 并行轨的关系

0.12.0（CLI 里程碑，`docs/cli-implementation-plan.md`）与 0.13.0 门禁并行推进。§15.1 "CLI 全部正式命令完成"、§15.6 "CLI exit code/stdout/stderr/machine schema 冻结为 v1 candidate" 与 RFC 0015 依赖 0.12.0 交付（工作区当前为 0.12.0 进行中状态：`crates/consema/src/bin/`、`crates/consema-protocol/src/cli.rs` 等）。0.13.0 门禁收口（§4 M9）必须在 0.12.0 合入后执行；本计划其余里程碑（M1-M8）不阻塞，可与 0.12.0 并行。

**0.12.0 并行轨状态（2026-08-07）：** M10（文档 + 发布门禁）已交付——CHANGELOG 0.12.0 条目、IMPLEMENTATION.md（CLI 章节 + semantic-model v7 + crate 边界表）、`docs/cookbook.md`、`docs/migration-guide.md`、`docs/BENCHMARKS-0.12.0.md`、README CLI 工作流示例、verify-package-archives 全量（14 个 `.crate` + MSRV 1.85 腿）通过；M9（`consema.cli.conformance@1` 向量套件）已落盘：`conformance/vectors/cli-v1.json` 40 个 case、`crates/consema-conformance/src/cli_v1.rs` 全套件通过（`cli_v1_suite_passes_fully` ok），18 套 suite 508/508；M9 收口完成：`cargo test --workspace --all-targets --all-features` 1,564 tests 全绿、clippy `-D warnings` 与 fmt 全绿（M10 顺带更新了 `cli_skeleton.rs` 的自检计数断言：conformance 自检随 M9 的 redact 自检增至 3 项）。§1.6 的 CLI 冻结两项随 M9 落定关闭，0.13.0 收口（§4 M9）复核即可。

---

## 1. 第 15 节门禁逐项清单（现状 → 缺口）

### 1.1 功能门禁（§15.1）

| 条目 | 现状 | 证据 |
|---|---|---|
| 八个 mandatory 格式家族与全部 mandatory Profile | 完成 | README.md:26-32；RFC 0009-0014 |
| §8 capability matrix 全部通过 | 完成（conformance 覆盖） | README.md:56（519/519；2026-08-12 补强前为 508/508） |
| PortableValue/PVCE、PortableGraph/PGCE | 完成 | `conformance/vectors/protocol-v1.json`、`portable-graph-v1.json` |
| native/syntax query、projection、materialization、provenance | 完成 | vectors：syntax-query-v1、operations-v1、source-v1、semantic-model-v5/v6 |
| scalar 与 mandatory structural edits | 完成 | README.md:29-31（各格式版本化编辑操作清单） |
| ChangeSet、SourcePatch、dry-run、batch manifest | ChangeSet/SourcePatch 完成；batch manifest 属 0.12.0（RFC 0015） | README.md:31；cli-implementation-plan.md:256-277 |
| CLI 全部正式命令 | 完成（0.12.0 合入 9c1ede2：11 命令 + cli suite 40 case） | CHANGELOG.md:9；RFC 0015 |
| 无 experimental/stub/partial mandatory capability | 完成（M9 复核：未接线面全部是显式拒绝，非 stub） | CHANGELOG.md:39-44（Boundaries）；cookbook §10 |

**结论：** 全部完成（M9 复核：`consema capabilities` 实测输出与 facade 类型派生一致——8 families / 16 profiles / 21 query domains / 16 operation registries / 187 codes（audit F3 注册 `json.projection.incomplete-document@1` 后 186 → 187），cli_m4.rs:160 断言相等）。

### 1.2 规范门禁（§15.2）

| 条目 | 现状 | 证据 |
|---|---|---|
| 每个公共行为有权威规范章节 | 完成 | 路线图 §4-§9 + RFC 0001-0015；docs/IMPLEMENTATION.md |
| 每个 contract ID 已登记且不可变 | 完成（v1-v6 冻结） | README.md:33（38 contract / 166 code）；`crates/consema-protocol/src/registry_manifest.rs` |
| public object 的 lifecycle/identity/completion/failure 已定义 | 完成 | docs/IMPLEMENTATION.md；SECURITY.md |
| 不依赖 Rust 类型名解释语言无关行为 | 完成（v7 为 0.12.0 并行轨） | cli-implementation-plan.md:496（machine schema 无 Rust 类型名） |
| provisional abstraction 已验证/修订/删除 | 完成 | 路线图 §0.2 记录；RFC 进程 |
| Go API mapping RFC 已通过 | 完成（M4：RFC 0016 立项/charter RFC 已接受；Go 实现 0.14.0 G0.1-G0.3 已按 2026-08-07 decision record 启动——fc-manifest decisions[0]，质量门禁全绿） | docs/rfcs/0016-go-api-mapping-v1.md；API-REVIEW §7 |
| 已知规范歧义有 resolution 或阻断状态 | 完成 | 各 RFC "rejected alternatives"；oracle exclusion inventory（plist D-1..D-21、HCL D-1..D-9） |

### 1.3 质量门禁（§15.3）

| 条目 | 现状 | 证据 |
|---|---|---|
| 全部 unit/integration/doc/conformance tests 通过 | 通过（M9 实测：`cargo test --workspace --locked` 1,617 passed / 0 failed；CI 落地后为常设） | docs/fuzz-evidence-0.13.0.md:155；CHANGELOG.md:34（1,617 passed / 0 failed 实测记录） |
| 上游/官方套件通过，差异有 Profile 说明 | 完成 | README.md:35-38；SECURITY.md:30 |
| Complete Document byte-exact round-trip | 完成 | vectors + 各格式 fixtures 测试 |
| edit untouched regions byte-exact | 完成 | SECURITY.md:26（UntouchedByteProof） |
| 未授权 lossy projection/materialization 拒绝 | 完成 | SECURITY.md:14-15；各格式 materialization 闭包验证 |
| 每 parser/decoder/query/projection/materialization/edit 持续 fuzz | 完成（M2：确定性 in-process 引擎 + 17 个长期 target，cargo-fuzz wrapper 双接线，corpus 种子 47 文件进 git） | crates/consema-conformance/src/fuzz.rs；crates/*/fuzz/；tests/*_fuzz.rs |
| 72 CPU-hours release-candidate fuzz，零未解释问题 | **部分（M8 账本）**：26.309 真实 CPU-hours、4,063 次进程运行、10.994 亿次变异、零新发现（2026-08-07 会话结束快照口径，追加式账本以 runs.csv 为准）；每格式家族 ≥72h 门槛未达（最接近的 properties 6.9%），完成路径见 fuzz-evidence §7 | docs/fuzz-evidence-0.13.0.md §3.2.1/§7/§8；logs/runs.csv |
| fuzz regression 永久加入 corpus | 完成（mutation-v1.json `regressions` 数组 + replay 测试 mutation_corpus.rs；当前零条目如实记录） | conformance/corpora/mutation-v1.json；fuzz-evidence §4 |
| protocol/varint/offset/graph/alias property tests + mutation review | 完成（M2：3 组 proptest + 46 fixtures × 174,921 变异 cases 全量 replay 通过） | tests/property_{graph,protocol,plist}.rs；conformance/corpora/mutation-v1.json |
| 无未分类 P0/P1，P2 有发布判断 | 完成（M2-F1 P1 / M2-F2 P0 均已修复并带严格断言/trip-wire；fuzz 会话零新发现） | fuzz-evidence §6；crates/consema-json/src/projection.rs:330；crates/consema-yaml/src/native.rs:497 |

### 1.4 安全门禁（§15.4）

| 条目 | 现状 | 证据 |
|---|---|---|
| `unsafe_code = "forbid"` | 完成 | Cargo.toml:37 |
| 无副作用链（parse/query/project/materialize） | 完成 | SECURITY.md:3-14；各格式 RFC 硬门禁 |
| XML external entity / YAML tag / HCL evaluation 默认关闭 | 完成 | SECURITY.md:32/34/36 |
| 所有输入/递归/节点/输出/诊断/匹配/编辑可限制 | 完成 | SECURITY.md:3-12 |
| resource limit 不产生截断假成功 | 完成 | SECURITY.md:14 |
| 无未接受 critical/high 漏洞 | 通过 | cargo audit 本地实测（1,189 advisories / 76 deps / 0 漏洞，2026-08-07）；CHANGELOG.md:36 |
| dependency license/来源/维护状态审计 | 通过 | cargo deny 四段本地实测；deny.toml |
| threat model、SECURITY.md、披露与修复流程 | 完成（M7：SECURITY.md 即 threat model 载体——攻击面/无副作用链/资源限制/硬化门禁；披露渠道 + 分级 SLA + 支持窗口落 SECURITY.md:40-48） | SECURITY.md:40-48；docs/support-policy.md §4 |
| CLI secret redaction 与临时文件权限跨平台验证 | **部分**：Windows 全量实测完成（0.12.0）；Linux/macOS 全量验证随 CI 第一次真跑（manifest 开放项 C-1） | CHANGELOG.md:14,34；docs/fc-manifest-0.13.0.json C-1 |

### 1.5 性能门禁（§15.5）

| 条目 | 现状 | 证据 |
|---|---|---|
| 线性/明确复杂度 | 完成（M5 复验：三处超线性路径修复 + 回归网；S1/S3/S4 后修复实测线性） | parser.rs:2027-2052,2842-2878；source.rs:466-509,1538-1589；offsets.rs:1-80 |
| adversarial 无超线性退化 | 完成 | hardening 测试；fuzz 10.994 亿次变异零超时（runs.csv 4,063 行，2026-08-07 会话结束快照） |
| 每格式 parse/render/query/projection/materialization/edit benchmark | 完成（0.6.0-0.11.0 六份基线 + 0.13.0 冻结表） | docs/BENCHMARKS-*.md |
| 固定硬件/固定 corpus/版本化 baseline | 完成 | 各 BENCHMARKS "Environment" 段；BENCHMARKS-0.13.0.md §1-§3 |
| p50/p95 时间与峰值内存正式预算 | 完成（M5 冻结：§4/§5/§6/§7 预算行，自 0.13.0 起为 release budget） | docs/BENCHMARKS-0.13.0.md |
| 超预算或相对上一基线 10% 回退有批准记录 | 完成（M5：§8 政策 + §9 模板 + APPROVALS 载体；后修复只改善性能，无触发） | BENCHMARKS-0.13.0.md §8/§9 |
| 大文档/深嵌套/大量重复/大量小节点独立场景 | 完成 | 各格式 baseline corpus 段 |
| lazy/streaming 的完成/取消/backpressure | 不适用（无 lazy/streaming capability） | 路线图 §24 非目标核对 |

### 1.6 API 与产品门禁（§15.6）

| 条目 | 现状 | 证据 |
|---|---|---|
| Rust public API 独立审查 | 完成（M4：五维审计全部转写 + disposition；B-6/B-9 修复含回归测试；semver 基线 RFC 级批准） | docs/API-REVIEW-0.13.0.md |
| backend AST 与第三方错误类型不泄漏 | 完成（facade 设计） | cli-implementation-plan.md:42-46（bin 只走 public API）；API-REVIEW §6 |
| facade/低层 crate/feature 关系清楚 | 完成 | crates/consema/Cargo.toml（facade 依赖 13 个 backend）；无 `[features]`；API-REVIEW §5 |
| MSRV 在 manifest 声明并在 CI 真正验证 | **部分**：声明（Cargo.toml:33）+ msrv job 落盘（ci.yml:118-144）+ 本地等效实测（M1 验证记录）；GitHub 真跑归开放项 C-1 | Cargo.toml:33；.github/workflows/ci.yml:118-144 |
| 全部 public API 有 rustdoc | 完成（M4：14 crate 零 missing-docs；M2 合入后 fuzz.rs 的 12 处已补齐） | API-REVIEW §4；fuzz.rs:52-69 |
| 每个主要工作流有可复制示例 | 完成（cookbook §12 每格式矩阵 + 6 个 baseline examples + pilot 实测） | docs/cookbook.md；M6 |
| CLI exit code/stdout/stderr/machine schema 冻结 v1 candidate | 完成（RFC 0015 冻结；cli suite 40 case；v7 随 0.12.0 合入） | RFC 0015；conformance/vectors/cli-v1.json |
| patch/apply 中断/冲突/权限/磁盘错误测试 | 完成（0.12.0 tests/cli_*.rs + pilot W8 实测） | crates/consema/tests/；pilot §2.8 |
| release artifact 可从干净环境重建 | **部分**：脚本本地全流程实测 exit 0（含 MSRV 腿）；CI package job 落盘；GitHub 真跑归开放项 C-1 | gate plan §4 M1 验证记录；ci.yml:285-303 |
| changelog/migration/compatibility/support policy | 完成（M6：support-policy.md；migration guide 0.12.0 M10；CHANGELOG 0.13.0 条目本里程碑） | docs/support-policy.md；docs/migration-guide.md；CHANGELOG.md |

### 1.7 Feature-Complete Manifest（§15.7）

**已交付（M9，本里程碑）**：`docs/fc-manifest-0.13.0.json`——机器可读（JSON 校验通过），逐条记录 §15.1-§15.7 门禁条目的状态（complete/partial/not_applicable）、证据（file:line）、语义权威来源，partial 条目附完成路径与 owner；判定 `not_closed`，开放项 C-1（CI 真跑）/C-2（72 CPU-hours fuzz）/C-3（真实发布密钥与发布执行）。Go 以该 manifest 为起点（§15.7 第 1445 行），0.14.0 不得在判定翻转前开始。

---

## 2. 已通过门禁的证据清单（2026-08-07 复核）

| # | 门禁 | 证据（file:line 或实测） | 复核方式 |
|---|---|---|---|
| P-1 | 18 套 suite 508/508（2026-08-12 P2-B 补强后 519/519） | README.md:56、CHANGELOG.md:17；本计划对 `conformance/vectors/*.json` 逐文件计数 = 18 suites / 508 cases（补强后 519） | 脚本计数（本计划 §4 M1 落为 CI 断言；现行断言 consema-rs/.github/workflows/ci.yml:191-209） |
| P-2 | conformance crate 全套测试 | `cargo test -p consema-conformance --locked` 实测 exit 0（58 lib + 74 integration） | 本地实测 2026-08-07 |
| P-3 | deny 四段 | `cargo deny check advisories bans licenses sources` 实测全绿 | 本地实测 2026-08-07（deny.toml） |
| P-4 | cargo audit | `cargo audit --no-fetch`：1,189 advisories、76 deps、0 漏洞（Cargo.lock 实测 76 个 `[[package]]`；cargo audit 口径随版本变化，以当前扫描结果为准） | 本地实测 2026-08-07 |
| P-5 | 硬化 55 个恶意/边界测试 | SECURITY.md:16；tests/hardening.rs 等 10 个 test 文件 | 测试通过（P-2） |
| P-6 | 上游套件 | README.md:35-38；SECURITY.md:30 | 发布记录 + oracle manifest |
| P-7 | 差分 oracle 存在且 skip 路径文档化 | 两个 manifest 的 `skip_path`；wrapper exit 3 | 读脚本（run-hcl-go-oracle.ps1:34-40、run-plist-macos-oracle.ps1:27-30） |
| P-8 | 性能基线六份 | docs/BENCHMARKS-0.6.0.md..0.11.0.md | 文件存在 |
| P-9 | unsafe forbid + missing_docs + MSRV 声明 | Cargo.toml:33、36-38 | 读 manifest |
| P-10 | 打包验证脚本（tar 路径安全 + 校验和） | scripts/verify-package-archives.ps1（90-170 行逻辑） | 本地实测（本次加固后全流程，见 §4 M1 验证记录） |
| P-11 | 版本锚点 v0.8.0 | `git tag -l`；git cat-file -p v0.8.0 | 本地 |

---

## 3. 缺口清单

| # | 缺口 | 对应门禁 | 计划里程碑 | 说明 |
|---|---|---|---|---|
| G-1 | 无 CI | §15.6（三平台 CI 为 §14.12 明列） | M1 | **已交付**：`.github/workflows/ci.yml`（10 job 落盘；GitHub 真跑归 manifest 开放项 C-1） |
| G-2 | verify-package-archives 无 `-AllowDirty`/无 MSRV 腿 | §15.6 干净重建 | M1 | **已交付**：`-AllowDirty` + 脏树前置条件 + MSRV 腿，本地干净 worktree 全流程 exit 0 |
| G-3 | MSRV 1.85 未在 CI 验证 | §15.6 "MSRV 在 manifest 中声明并在 CI 真正验证" | M1 | **已交付（job）**：msrv job（ci.yml:118-144，同门禁全跑）；真跑归 C-1 |
| G-4 | 无 fuzz 目标、无 property-test 框架、无 mutation 语料、无 72 CPU-hours 证据 | §15.3 | M2、M8 | **已交付**：17 target + 3 组 proptest + 46 fixtures × 174,921 mutation cases（M2）；72h 累计部分——账本 26.309 CPU-hours、4,063 行、零新发现（M8，2026-08-07 会话结束快照） |
| G-5 | coverage 工具链缺失，84.65% 不可复现 | §15.3/§18.3 | M3 | **已交付**：`scripts/coverage.ps1` + `docs/COVERAGE-0.13.0.md`（86.51/82.82/87.91，脚本产出 + 硬下限 + -Trend） |
| G-6 | 性能/内存正式预算与回退政策未冻结 | §15.5、§20.3 | M5 | **已交付**：BENCHMARKS-0.13.0.md 预算冻结 + §8/§9 政策 + 三处超线性修复复验 |
| G-7 | cargo-semver-checks 无配置 | §15.6 "Rust public API 完成独立审查" | M1（基线）/M4（审查） | **已交付**：semver job + API-REVIEW §3 基线解读（8 全绿 + 3 仅新家族变体，RFC 级批准） |
| G-8 | 五维审计 naming-drift findings F2/F3/F4/F10/F11 等未转写、未处理；Go API mapping RFC 未立项 | §15.6、§15.2 | M4 | **已交付**：API-REVIEW-0.13.0.md（7 findings + 2 philosophy + B-1..B-9 全部 disposition；B-6/B-9 修复）；RFC 0016 立项 |
| G-9 | 真实项目 pilots 与 §23 必做工作流 | §15.6、§22.7 | M6 | **已交付**：docs/pilot-0.13.0.md（W1-W8 实测 + §23.3 指标 12 项；F-2 性能发现已解决） |
| G-10 | 发布供应链：SBOM、签名标签、build provenance、恢复演练、披露流程 | §19.4、§15.4 | M7 | **已交付**：release-process-0.13.0.md + release-sign/sbom 脚本 + 三场演练记录 + SECURITY.md:40-48 披露章节；真实发布密钥归 C-3 |
| G-11 | Feature-Complete Manifest 不存在 | §15.7 | M9 | **已交付（本里程碑）**：docs/fc-manifest-0.13.0.json（判定 not_closed，开放项 C-1/C-2/C-3） |
| G-12 | cookbook/migration/support policy 文档 | §15.6 | M6 | **已交付**：cookbook（每命令真实输出 + 每格式能力矩阵 + loss policy 示例）、migration-guide（0.8.0 API → 0.12.0 facade + CLI、v6→v7）、support-policy（§21.4 七必公开项） |
| G-13 | 0.12.0 依赖项：CLI 全命令、v7 冻结、batch apply | §15.1/§15.6 | 并行轨 + M9 收口 | **已交付**：0.12.0 合入 9c1ede2（CLI 11 命令 + v7 + cli suite 40 case）；M9 复核完成（§1.1 F-6/F-7/F-8） |

---

## 4. 里程碑拆分

### M1 — CI 三平台工作流与打包门禁收口（串行起点；**已交付 2026-08-07**）

范围：
- `.github/workflows/ci.yml`（已创建；六仓拆分 2d7494f 后归 consema-rs 仓，母仓 ci.yml 为 oracles/shared-conformance-digest/check 三 job）：10 个 job——lint（3 OS × stable）、test（3 OS × stable）、coverage（硬下限 + `-Trend` 趋势门禁）、msrv（ubuntu × 1.85，跑同门禁）、conformance（suite 计数断言 18/508，补强后 18/519）、deny、audit、semver（baseline v0.8.0）、oracles（文档化 skip=成功）、package（含 MSRV 腿）。
- `scripts/verify-package-archives.ps1`（已加固）：`-AllowDirty` 开关 + 脏树前置条件错误（列出脏文件）；`-SkipMsrv` 开关 + 默认开启的 MSRV 构建腿（rust-version 从 cargo metadata 读取，工具链缺失以明确消息失败）。
- 收口动作：推入 GitHub 后在干净 checkout 上跑全矩阵，修复任何失败（例如 MSRV 下 clippy 差异、oracle 版本 pin 与 runner 镜像不符——见 R-2/R-3）。

验收门禁：10 个 job 全绿；`python -c "import yaml; yaml.safe_load(...)"` 通过；PowerShell parse 通过；本地验证记录见 §4 M1 验证记录。

**M1 验证记录（本计划执行时，2026-08-07，全部实测）：**
- `.github/workflows/ci.yml`：PyYAML safe_load 通过（10 jobs，矩阵正确）。
- 脚本 PowerShell 语法 parse 通过（PS 5.1 Parser API）。
- 脏树前置条件（多脏文件与单脏文件两态）：不带 `-AllowDirty` 运行 → 报错并逐行列出现场脏文件（含本计划新增文件），不触发 cargo package。
- 脏树 + `-AllowDirty`（本地 1.85.0）：cargo package 14 个可发布 crate 成功 → tar 路径安全检查 → 校验和检查 → 每 crate offline check 全绿 → MSRV 腿正确拒绝 0.12.0 进行中代码（`crates/consema/src/bin/consema/detect.rs:123` 使用 let-chains，需要 rustc ≥1.88；workspace 声明 rust-version 1.85）——MSRV 门禁按设计工作，该缺陷归 0.12.0 并行轨修复。
- 干净 HEAD 工作树（git worktree）全流程 exit 0：cargo package → tar 安全 → 校验和 → 14 crate offline check → MSRV 腿 `rustc 1.85.0` 对全部 14 crate `build --all-targets --all-features` 全绿，输出 "verified 14 publishable package archives" 与 "MSRV build leg: rustc 1.85.0 for all 14 crates"。附带发现：MSRV 腿在 consema-hcl test target 报 1 个 unused-variable warning（1.85 下新出现，不失败），列入 0.13.0 收口清理项。
- 注：`cargo +1.85`（部分版本号）会触发 rustup 频道同步（网络 + stderr，PS 5.1 下 EAP=Stop 会抛 NativeCommandError），因此脚本用 `rustup toolchain list` 解析精确版本（如 1.85.0）后以 `+1.85.0` 调用（无同步、无 stderr）。

### M2 — fuzz/property/mutation 语料（‖，**已交付 2026-08-07**）

范围：
- 每格式 fuzz 目标（cargo-fuzz 或等价 harness）：parser/decoder/query/projection/materialization/edit 各入口；资源上限固定为生产 Profile 默认值。
- property-test 框架补位：protocol/varint/offset/graph/alias 高风险逻辑（§15.3 第 1384 行）的 proptest 属性测试。
- mutation 语料：对 fixtures 的逐字节/逐 token mutation 入 `conformance/corpora/`（现有 json5-v2.2.3.json 先例），fuzz regression 永久入 corpus。
- 每 target 的 corpus 与 seed 目录进 git（不发随机种子的空跑不算证据）。

验收门禁：每格式 fuzz target 存在且 `cargo test` 全绿；首次 fuzz 运行无未解释 panic/hang/limit bypass；regression 入 corpus 的流程文档。

### M3 — coverage 工具链（‖，**已交付 2026-08-07**）

范围：cargo-llvm-cov 常设脚本（`scripts/coverage.ps1`，仓库体例），统一 target 与 corpus；把 CHANGELOG.md:92 的 84.65% 替换为可复现报告；按 §18.3 定 coverage 政策（语义证明不替代 coverage；定阈值或趋势门禁，不伪造数字）。

验收门禁：`scripts/coverage.ps1` 可复现输出（固定 commit）；报告数值入库。

### M4 — API/semver 审查（‖，依赖 M1 的 semver job 与 G-8；**已交付 2026-08-07**）

范围：
- 转写五维审计 naming-drift findings（F2/F3/F4/F10/F11 等，来源：审计会话记录——仓库内无档案，见 G-8）到 `docs/API-REVIEW-0.13.0.md`，逐条给出 disposition（修复/豁免+理由）。
- cargo-semver-checks 基线（M1 已建）结果解读：任何相对 v0.8.0 的 breaking change 必须在此里程碑给出 RFC 级决定。
- rustdoc 覆盖率断言、facade/feature 关系复查（§15.6 第 1416-1421 行）、backend AST/错误类型泄漏复查。
- Go API mapping RFC 立项（§15.2 第 1371 行）。
- **0.12.0 M10 移交的 CLI API 评审 backlog（2026-08-07 复核，来源：CLI milestone M5/M7/M8 文档化边界 + M10 评审发现）**，逐条处理并写入 `docs/API-REVIEW-0.13.0.md`：

| # | 项 | 证据（file:line） | M10 disposition |
|---|---|---|---|
| B-1 | query 只接线 `core.portable-value-query@1`；native 域（及 xml/plist/hcl 源）需调用方外部化 node locator，facade 未暴露该 API | `crates/consema/src/bin/consema/query_cmd.rs:65-67,1064-1093` | backlog（修复 = facade locator API 面，非廉价；与 B-2 同一根因） |
| B-2 | materialize/convert 结果的 provenance map 为空（同因：facade 无 locator API） | `materialize_cmd.rs:170-178`（注释明示 "a facade locator API is the M10 review item"） | backlog（与 B-1 合并为一个 facade locator API 项） |
| B-3 | project 报告/来源外部化仅 json/toml（其余家族显式拒绝；TOML 非空报告拒绝） | `project_cmd.rs:14-20,248-257,679-697` | backlog（每格式报告外部化映射）；显式拒绝行为保留 |
| B-4 | 信封只携带注册 code：格式本地 code 绑定注册 fallback `core.source.invalid-sequence@1`，stderr 保留真码 | `query_cmd.rs:150-166` | 保持（RFC 0015 §4.3 冻结语义；换 fallback 码需 RFC 修订，评审时复核 fallback 选择是否值得专设 code） |
| B-5 | 失败的 `cli.convert@1` 记录形态 report/target 为 null，失败事实只在诊断 | `convert_cmd.rs:252-268,417-436` | backlog（评审是否在失败形态中外部化各阶段 report） |
| B-6 | **java-properties 源的 project/convert 不可达**（M10 评审发现，非文档化边界）：`format_family` 对 java-properties 返回 `properties`，`wire_projection_request` 族前缀检查按 `properties.projection.` 拒绝 `java-properties.projection.*` 目标 | `query_cmd.rs:893-905`（format_family）与 `project_cmd.rs:48-90`（族前缀检查） | backlog 优先修复项（bug，非设计；修复 ≈ 2 行族前缀特例 + 回归测试） |
| B-7 | edit/plan/apply 操作词表仅 INI family | `edit_cmd.rs:47-53,510-519` | backlog（每格式操作请求映射是大面） |
| B-8 | `edit --write` 未接线（dry-run only；M8 计划接受标准 "edit_cmd.rs 的 --write 复用同一引擎" 未交付） | `edit_cmd.rs:1023,1063`；测试钉死 `crates/consema/tests/cli_m7_plan.rs:98`；帮助文本已于 2026-08-07 修订（`args.rs:194` 改为 "dry-run only"、`:214` 的 --write 行已删），行为不变（usage 显式拒绝） | backlog 优先修复项（接线 fsio 提交路径，或修订 RFC 0015 §6.1 edit 行与帮助文本；帮助文本修订已随 2026-08-07 落地） |
| B-9 | **`inspect --profile` 对携带未注册格式本地 code 的诊断（xml/plist/hcl 家族、以及 category 与注册表矛盾的 INI/Properties 恢复诊断）exit 5 内部错误**（M10 评审发现，非文档化边界）：inspect 直接 `DiagnosticMessage::from_core_with_registry` 绑定，未走 query_cmd 的 `registered_code` fallback；应报告 Recovered/fatal 事实（RFC 0015 §7.2，exit 0/2 语义），不是 internal | `crates/consema/src/bin/consema/inspect.rs:355-370,382-395`（对比 `query_cmd.rs:150-166` 的 fallback）；实测：`inspect bad.xml --profile xml.1.0-safe` → exit 5 `xml.tree.mismatched-end-tag@1`；`inspect rec.ini --profile ini.portable`（Recovered）→ exit 5 | backlog 优先修复项（复用 `registered_code` 模式 + 回归测试：xml/plist/hcl/ini Recovered 与 fatal 的 inspect 全矩阵） |

验收门禁：`docs/API-REVIEW-0.13.0.md` 完成且每条 finding（含 B-1..B-9）有 disposition；semver job 全绿（或批准记录）；rustdoc 100% 断言。

### M5 — 性能与内存预算冻结（‖，基于 BENCHMARKS-0.11.0 及更新；**已交付 2026-08-07**）

范围：`docs/BENCHMARKS-0.13.0.md`（照 BENCHMARKS-0.9.0.md 体例）冻结每格式 parse/render/query/projection/materialization/edit 的 p50/p95 与峰值内存预算；§20.3 回退政策落成批准记录流程；大文档/深嵌套/大量重复/大量小节点场景按 §15.5 第 1409 行单列。

验收门禁：预算表发布（版本化）；任何超预算或 10% 回退有批准记录；CI 或定期复核可执行。

### M6 — 文档/examples/真实项目 pilot（‖，**已交付 2026-08-07**）

范围：cookbook（每格式 inspect/query/project/edit 示例，§22.6）；migration guide；support policy（§21.4）；§23 真实项目 pilot 工作流（至少一个真实配置项目按 §23.2 必做工作流跑通，输出 §23.3 核心指标）。

验收门禁：每个主要工作流有可复制示例（§15.6 第 1421 行）；pilot 报告含 §23.3 指标。

### M7 — 发布供应链与恢复演练（‖，§19.4；**已交付 2026-08-07**）

范围：
- SBOM：选型（cargo-sbom / cyclonedx-bom，记录 rejected alternatives）并产出机器可读 SBOM。
- 签名：发布 tag 与 release artifact 的 GPG 签名流程（现有标签未签名，见 P-11/G-10）；checksum 清单（复用 verify-package-archives.ps1 的 sha256 输出）。
- build provenance：干净环境重建步骤文档化 + CI package job 作为常设载体（M1 已建）。
- 恢复演练：损坏归档/校验和失败/丢失标签的恢复步骤，至少一次演练记录。
- 披露流程与支持周期文档（§19.4 第 1764 行）。

验收门禁：SBOM 可复现生成；签名流程有演练记录；恢复演练记录入库。

### M8 — 72 CPU-hours release-candidate fuzz（串行，依赖 M2；**账本已开 2026-08-07，72h 门槛未达**）

范围：每格式 release-candidate target 累计 ≥72 CPU-hours，零未解释 crash/panic/hang/limit bypass；新 crash 清零该 target 计时（§15.3 第 1387 行）；全部 regression 入 corpus；P0/P1 清零、P2 有发布判断。

验收门禁：累计时长证据（机器日志）+ 全部 corpus 全绿 + 缺陷分类记录。

### M9 — Feature-Complete Manifest 与门禁收口（串行，依赖全部；**已交付 2026-08-07**）

范围：按 §8 产出 `docs/fc-manifest-0.13.0.json`；跑 §7 验收门禁总表并记录；0.12.0 合入后的 CLI/v7 项复核（§1.1）；threat model 与披露流程收口（§1.4）；门禁记录写入 CHANGELOG（发布体例）。

验收门禁：§7 表格全部 ☑；manifest 机器可读且 digest 可复验；0.14.0 Go 起点就绪。

**M9 交付记录（2026-08-07）：**

- 产出 `docs/fc-manifest-0.13.0.json`（本计划 §8 草案形状；`python -m json.tool` 校验通过，无替换字符）：6 组门禁逐条状态 + 证据（file:line）+ 语义权威来源；partial 条目带完成路径与 owner；判定 `not_closed`。
- §7 验收门禁总表逐行核对（见 §7 新增状态列）：完成项 13 行、部分项 3 行（10-job CI 真跑、verify-package-archives CI 真跑、fuzz 72h）、无未核对行。
- 0.12.0 合入（9c1ede2）后的 §1.1 复核：F-1..F-8 全部 complete（CLI 11 命令 + `consema capabilities` 实测 8 families / 16 profiles / 21 query domains / 16 operation registries / 187 codes（audit F3 注册 `json.projection.incomplete-document@1` 后 186 → 187），与 cli_m4.rs:160 断言一致）；Boundaries（B-1/B-3/B-4/B-5/B-7/B-8）为显式拒绝而非 stub（F-8）。
- 门禁记录写入 CHANGELOG.md（`## Unreleased — 0.13.0`：gate 基础设施、Correctness 修复、Verified 实测、Boundaries 的 partial 诚实陈述）。
- **semver job 二选一落定**：API-REVIEW §3 的"或保留失败并附批准记录"被选为默认（ci.yml 属 `.github` 文件域，本里程碑不改；三个 crate 的 `enum_variant_added` 有 RFC 级批准记录，推入时由 gatekeeper 决定是否加 allowlist——manifest 开放项 C-1 已记录）。

**M9 之后仍然开放（manifest 开放项 C-1/C-2/C-3，全部有完成路径与 owner）：**

| 开放项 | 门禁条目 | 现状 | 完成路径 |
|---|---|---|---|
| C-1 | A-4/A-9/SEC-9（CI 真跑） | ci.yml 10 job 落盘；本地等效实测全绿；GitHub 真跑未发生 | 推入 GitHub 后干净 checkout 全矩阵全绿；oracle 保持文档化 skip=成功（R-2/R-3） |
| C-2 | Q-7（72 CPU-hours fuzz） | 账本 26.309 CPU-hours、4,063 行、零新发现（2026-08-07 会话结束快照口径，追加式账本以 runs.csv 为准）；每格式家族门槛未达（最接近 properties 6.9%） | fuzz-evidence §7：clang 主机 cargo-fuzz 17 target 为主，本机确定性协议续跑为备选；追加式账本 |
| C-3 | SEC-8 发布物侧 + 版本推进 | 演练密钥完成；真实发布密钥未生成；workspace 版本 0.8.0（2026-08-07 时点快照；workspace 现为 1.0.0-rc.1，commit 2209582） | release-process §7 检查单 10 项（含真实密钥 + 备份 + 吊销证书） |

---

## 5. 多 agent 并行化建议（文件域划分）

| Agent | 文件域 | 里程碑 | 前置 |
|---|---|---|---|
| A | `.github/workflows/ci.yml`、`scripts/verify-package-archives.ps1` | M1 | 无（最先） |
| B | `crates/*/fuzz/`（新）、`conformance/corpora/`、`tests/*_fuzz.rs`、`tests/*_property.rs` | M2 | M1 的门禁可跑 |
| C | `scripts/coverage.ps1`、coverage 报告文件 | M3 | M1 |
| D | `docs/API-REVIEW-0.13.0.md`、五维审计转写、Go API mapping RFC | M4 | M1（semver job） |
| E | `docs/BENCHMARKS-0.13.0.md`、预算表 | M5 | 无 |
| F | `docs/cookbook.md`、`docs/migration.md`、pilot 记录 | M6 | 无 |
| G | `scripts/release-*.ps1`、SBOM、签名/演练记录 | M7 | 无 |
| H | fuzz 证据记录、corpus 更新 | M8 | M2 |
| I | `docs/fc-manifest-0.13.0.json`、CHANGELOG、门禁收口 | M9 | M2-M8 |

约束：B/D/H 的 corpus 与 review 文档是唯一权威载体（向量/corpus 与 runner 同批体例，cli-implementation-plan.md:416-417）；E 不得改动 BENCHMARKS-*.md 既有基线（只冻结新预算表）；并行里程碑之间无共享写域。

---

## 6. 风险清单

| # | 风险 | 说明与缓解 | 复核时机 |
|---|---|---|---|
| R-1 | 72 CPU-hours 证据成本 | 多 target 并行机器累计；日志留档；新 crash 清零规则（§15.3）可能拉长计划 | M2、M8 |
| R-2 | plist macOS oracle 版本 pin 与 runner 不符 | manifest pin macOS 26.5/Xcode 26.6/Swift 6.3.3；若 macos-latest 不匹配则 oracle throw → CI 红。缓解：pin 具体 runner label（macos-26 等）或自托管；pin 更新按 manifest "update together with runtime pins" 政策 | M1 |
| R-3 | HCL Go oracle 在 hosted runner 只能 skip | go.os=windows、windows_build=10.0.26200.0 需匹配 Windows 26200 主机；hosted runner 无此 pin。缓解：文档化 skip（exit 3）已按成功接线；真实差分运行归自托管 | M1 |
| R-4 | cargo-semver-checks 基线映射边界 | v0.8.0 后新增 crate（xml/plist/hcl）与 publish=false crate 不在对比内；M4 需显式记录该覆盖边界 | M1、M4 |
| R-5 | MSRV 1.85 与稳定工具链漂移 | clippy/rustc 新 lint 与新版语法只出现在稳定链上；已实测 0.12.0 进行中代码（consema bin detect.rs:123）使用 let-chains（rustc ≥1.88），MSRV 腿正确拦截。任何 MSRV 提升必须走 manifest 变更记录（§12 版本治理）；0.12.0 并行轨须在合入前修掉 1.85 不兼容代码 | 每个里程碑 |
| R-6 | fuzz 发现 P1/P2 的处理 | 缺陷等级定义见 §18.4；P0/P1 阻塞、P2 发布判断记录；发现即入 regression corpus | M8 |
| R-7 | 0.12.0 并行轨未按期合入 | M9 收口依赖 CLI/v7 项；M1-M8 不阻塞。若 0.12.0 延后，门禁清单中依赖项保持 open 并记录 | M9 |
| R-8 | 脏树/未跟踪文件混入门禁运行 | verify-package-archives 已加 -AllowDirty 与脏树报错；CI 恒在干净 checkout 运行 | M1 起 |
| R-9 | SBOM/签名工具选型 | cargo-sbom 与 cyclonedx-bom 等记录 rejected alternatives；SBOM 内容以 §19.4 清单为准 | M7 |
| R-10 | coverage 数字被误用为质量证明 | §18.3 "Coverage 不替代语义证明"；报告定位为回归探测器，阈值政策由 M3 定并写清 | M3 |

---

## 7. 验收门禁总表

| 门禁 | 体例来源 | 适用里程碑 | 状态（M9 核对 2026-08-07） |
|---|---|---|---|
| 10 job CI 全绿（3 OS × stable + coverage + MSRV 1.85 + conformance/deny/audit/semver/oracles/package） | §14.12、§15.6 | M1 起每个里程碑 | **部分**：10 job 落盘（ci.yml）+ 本地等效实测；GitHub 真跑归 C-1 |
| `cargo test --workspace --locked`（3 OS + MSRV） | §15.3 | M1 起 | ☑ 完成：1,617 passed / 0 failed 实测（fuzz-evidence:155）；MSRV 腿本地实测（M1 验证记录） |
| fmt + clippy `-D warnings` + rustdoc `-D warnings`（3 OS + MSRV） | 仓库开发门禁 | M1 起 | ☑ 完成（本地实测 + ci.yml job 落盘） |
| conformance 18 套 suite 508/508 + hardening + fixtures（suite 计数断言） | README/CHANGELOG 体例 | M1 起 | ☑ 完成（18/508 实测，2026-08-12 P2-B 补强后 519；ci.yml:155-173 为拆分前母仓行号，拆分后 suite-count 断言在 consema-rs/.github/workflows/ci.yml:191-209） |
| `cargo deny check` 四段 + `cargo audit`（RustSec DB） | SECURITY.md:38 | M1 起 | ☑ 完成（本地实测全绿 + jobs 落盘） |
| cargo-semver-checks vs v0.8.0 基线 | §15.6 | M1 起 | ☑ 完成（8 全绿 + 3 仅新家族变体，RFC 级批准；CI job 保持失败 + 批准记录，C-1 落定 allowlist） |
| 差分 oracle（文档化 skip=成功，exit 3） | oracle manifest `skip_path` | M1 起 | ☑ 完成（skip 接线 + 29/7 case 记录；hosted runner 行为随 C-1 复核） |
| verify-package-archives 含 MSRV 腿 | §15.6 干净重建 | M1 起 | **部分**：本地全流程 exit 0（14 归档 + 1.85 腿）；CI package job 真跑归 C-1 |
| fuzz 目标全集 + 72 CPU-hours RC 证据 + regression 入 corpus | §15.3 | M2、M8 | **部分**：target 全集/regression 机制完成；72h 累计 26.309（C-2，2026-08-07 会话结束快照） |
| coverage 可复现报告 | §18.3 | M3 | ☑ 完成（COVERAGE-0.13.0.md：86.51/82.82/87.91） |
| API 审查记录（F2/F3/F4/F10/F11 等 disposition）+ Go API mapping RFC | §15.6、§15.2 | M4 | ☑ 完成（API-REVIEW-0.13.0.md + RFC 0016） |
| 性能/内存预算冻结 + 回退批准记录 | §15.5、§20.3 | M5 | ☑ 完成（BENCHMARKS-0.13.0.md §4-§9 + 三处超线性修复复验） |
| cookbook/migration/support policy + 真实项目 pilot 指标 | §15.6、§23 | M6 | ☑ 完成（cookbook/migration/support-policy/pilot-0.13.0.md） |
| SBOM + 签名标签/artifact + checksum + 干净重建 + 恢复演练记录 | §19.4 | M7 | ☑ 完成（release-process-0.13.0.md + 演练记录 + sbom 产物；真实密钥归 C-3） |
| threat model + 披露流程 | §15.4 | M7、M9 | ☑ 完成（SECURITY.md:40-48 + SECURITY.md 全文档） |
| Feature-Complete Manifest 发布 + 全部上表 ☑ | §15.7 | M9 | **部分**：manifest 已发布（fc-manifest-0.13.0.json，JSON 校验通过）；☑ 未全（C-1/C-2/C-3 开放，判定 not_closed） |

---

## 8. Feature-Complete Manifest 草案（§15.7 第 1431-1443 行）

产出：`docs/fc-manifest-0.13.0.json`（机器可读；随门禁收口签名入库）。字段与事实来源：

| 字段 | 值来源 |
|---|---|
| `product_version` | workspace Cargo.toml version（当前 0.8.0，收口时按 §12 版本治理推进） |
| `spec_revision` | 路线图文档 + RFC 0001-0015 清单 digest |
| `contract_registry_digest` | consema-protocol `RegistryManifest::current()`（README.md:33：38 contract / 166 code）序列化后 sha256 |
| `capability_set` | `consema capabilities` 输出（0.12.0 交付） |
| `conformance_suite_digest` | `conformance/vectors/*.json`（18 文件）逐文件 sha256 聚合（M1 suite-count 断言同源） |
| `corpus_test_suite_revisions` | toml-test v2.2.0、yaml-test-suite data-2022-01-17（commit 6e6c296）、JSON5 v2.2.3、oracle manifests（含 hcl go1.26.5/plist macOS 26.5 pins） |
| `benchmark_baseline_revision` | docs/BENCHMARKS-0.13.0.md 所记 commit（M5） |
| `rust_compiler_msrv` | CI 实测 rustc 版本 + 1.85（M1 msrv job 证据） |
| `dependency_lock_digest` | Cargo.lock sha256（verify-package-archives 校验和同源） |
| `supported_targets` | CI 矩阵：windows-latest / ubuntu-latest / macos-latest |
| `known_accepted_limitations` | 各 Profile exclusion inventory（HCL D-1..D-9、plist D-1..D-21、YAML 1 项）序列化摘要 |

Go（0.14.0）以该 manifest 为起点，不以本地工作树偶然状态为起点（§15.7 第 1445 行）。

---

## 9. 本次交付物清单（与本文档同批）

1. `.github/workflows/ci.yml`（新）：M1 的 CI 初版（10 job，3 OS × stable + coverage + MSRV 1.85 腿；conformance suite-count 断言；deny/audit/semver/oracles/package）。
2. `scripts/verify-package-archives.ps1`（加固）：`-AllowDirty`（脏树前置条件报错 + `--allow-dirty` 透传）、`-SkipMsrv` + 默认开启的 MSRV 构建腿（rust-version 读自 cargo metadata，工具链缺失明确报错）。
3. `docs/0.13.0-gate-plan.md`（本文档；M9 里程碑已按上文更新为交付状态）。
4. `docs/fc-manifest-0.13.0.json`（M9，新）：Feature-Complete Manifest（§15.7），逐条门禁状态/证据/完成路径；判定 not_closed（开放项 C-1/C-2/C-3）。
5. CHANGELOG.md（M9）：新增 `## Unreleased — 0.13.0` 条目（gate 基础设施 + Correctness + Verified + Boundaries 的 partial 陈述）。
